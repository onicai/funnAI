/**
 * Frontend feature flags.
 * Flip these to enable/disable product surfaces without a canister upgrade.
 * Re-enable marketplace by setting MARKETPLACE_ENABLED to true after the issue is resolved.
 */
export const MARKETPLACE_ENABLED = false;

export const MARKETPLACE_DISABLED_MESSAGE =
  'The marketplace is temporarily unavailable and will return soon.';
